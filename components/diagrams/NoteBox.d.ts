/** Optional diagram callout for non-obvious constraints — white fill, thin pink border, bold pink "Note:" prefix. */
export interface NoteBoxProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
}
export declare function NoteBox(props: NoteBoxProps): JSX.Element;
