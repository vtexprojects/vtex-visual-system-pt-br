Optional callout box for diagrams — only when there is a non-obvious constraint worth flagging.

```jsx
<NoteBox>External systems only reachable through the middleware — no direct integration.</NoteBox>
```
