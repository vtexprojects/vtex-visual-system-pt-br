Button for VTEX Admin / product-UI mockups. Primary action color is Action Blue #134CD8 — pink is emphasis-only in the product.

```jsx
<AdminButton>Save</AdminButton>
<AdminButton variant="secondary">Cancel</AdminButton>
<AdminButton variant="danger">Delete</AdminButton>
<AdminButton disabled>Save</AdminButton>
```
