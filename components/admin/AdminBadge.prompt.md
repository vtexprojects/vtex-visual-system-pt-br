Status badge for Admin mockups (order status, stock state, etc).

```jsx
<AdminBadge type="success">Invoiced</AdminBadge>
<AdminBadge type="warning">Pending</AdminBadge>
<AdminBadge type="danger">Canceled</AdminBadge>
<AdminBadge>Draft</AdminBadge>
```
